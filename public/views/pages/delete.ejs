<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>deleteajax</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.css" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js" integrity="sha384-ygbV9kiqUc6oa4msXn9868pTtWMgiQaeYH7/t7LECLbyPA2x65Kgf80OJFdroafW" crossorigin="anonymous"></script>
    <style>
      body {
       background-color: #fafafa;
       font-family: Consolas;
       font-size: 18px;
       color:#2A2A2A;
      }
      .container {
       margin: 10px 40px;
      }
     </style>
     <link rel="stylesheet" href="../styles/nav.css">
     <script src="../scripts/navbarloginscript.js"></script>
</head>

<body>
  <%- include('../navbar/nav.ejs', { user: user }); %>
  <div class="container">
  <h1>DELETE</h1>
  <h3>example</h3>

  <p>function deleteajax()
    <br>{
      <br>console.log("DELETE");
      <br>$.ajax({
        <br>type: "DELETE",
        <br>url: "https://stankinlab-api.herokuapp.com/dis/286841271463444480",
        <br>success: function(msg){
          <br>console.log(msg);
          <br>}
          <br>});
          <br>}
  </p>

  <h3>processing</h3>

  <p>app.delete('/dis/:UserId', (request, response) => {
    <br>try
    <br>{
      <br>var UserId = parseInt(request.params.UserId);
      <br>var index = file.data.findIndex(i => i.UserId == UserId);
      <br>if(index == -1)
      <br>{
        <br>response.status(400).send("id not founded");
        <br>return;
        <br>}
        <br>file.data.splice(index,1);
        <br>pushToJson();
        <br>response.send("DELETE ok");
        <br>}
        <br>catch
        <br>{
          <br>response.send("error");
          <br>}
          <br>});
</p>

  </div>
 </body>
 </html>